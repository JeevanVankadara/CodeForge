import React from 'react'

const HomePage = () => {
  return (
    <div>
      /* contains a form to create a room and the username who is entering. On entering the room, it should enter into the room directly*/
      /* And also contains a join room with the link along with it name too. */
    </div>
  )
}

export default HomePage
